# FreeBeeGee demo (serverless) mode

This directory holds files needed for FreeBeeGee to be run in demo mode. They are not used during regular build, but when built in 'demo' mode, those will override the
